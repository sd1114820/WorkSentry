<Window x:Class="WorkSentry.Client.CloseToTrayTipWindow" x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="提示"
        Width="420" Height="220"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="#F3F4F6"
        FontFamily="Segoe UI, Microsoft YaHei">
    <Border Margin="16" Background="White" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="6" Padding="16">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock Text="已最小化到托盘" FontSize="18" FontWeight="SemiBold" Foreground="#111827" />
            <TextBlock Grid.Row="1" Text="关闭窗口将最小化到托盘，可在托盘右键退出。" Margin="0,8,0,0" FontSize="13" Foreground="#4B5563" TextWrapping="Wrap" />

            <Grid Grid.Row="2" Margin="0,16,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <CheckBox x:Name="RememberCheck" Content="记住，不再提示" Foreground="#374151" FontSize="12" />
                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
                    <Button x:Name="OkButton" Content="确定" Width="96" Height="34" Margin="0,12,0,0" Background="#2563EB" Foreground="White" BorderBrush="#2563EB" />
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
